<?php  
require_once 'sql.php';
function creat( $table ) {
	$conn = new PDO( DB_DSN, DB_USERNAME, DB_PASSWORD );
	$sql = "CREATE TABLE IF NOT EXISTS ".mysql_escape_string($table);
	$st = $conn->prepare( $sql );
	$st->execute();
	$conn = null;
}